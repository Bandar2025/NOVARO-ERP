# NOVARO ERP → Intelligent Business Platform
## وثيقة التدقيق الشامل للنظام القائم (Phase 0 Audit Report)
**تاريخ التدقيق:** 2026-09-13  
**الحالة:** مكتمل ومُعتمد للمرحلة 0  
**الدور الهندسي:** Principal ERP Architect + Accounting Systems Architect + Senior Full-Stack Engineer  
**النطاق:** فحص شامل للشيفرة المصدرية، البنية المعمارية، دورات المحاسبة، المخزون، الإنتاج، والديون التقنية.

---

### 1. ملخص تنفيذي (Executive Summary)
مشروع **NOVARO ERP** هو نظام لإدارة الموارد موجه لشركات الأغذية والمحامص والمصانع التجارية، مبني باستخدام React 19 + TypeScript + Vite 6 و Tailwind CSS v4، ومُدار عبر خادم Node.js Express (`server.ts`).

**النتيجة الرئيسية للتدقيق:**
النظام الحالي يتمتع بواجهات مستخدم عربية/إنجليزية غنية ومكونات واجهة متقدمة (مثل `ERPTable.tsx`)، إلا أنه يعاني من **معمارية متجانسة محلياً (Local Monolith)** تعتمد بالكامل على `StateContext.tsx` الممتد لأكثر من 1,850 سطراً مع تخزين كامل للبيانات في متصفح العميل عبر `localStorage`. كما تم اكتشاف **تفاوتات حرجة في المحاسبة وحساب تكلفة البضاعة المباعة (COGS)** داخل التقارير (حسابات تقريبية بنسب مئوية ثابتة بدلاً من القيد المحاسبي المزدوج)، بالإضافة إلى **فصل غير مكتمل في أنواع وظائف التحميص والطحن** أدى إلى تعطل حفظ تشغيل المحامص والمطاحن برمجياً بسبب تضارب أسماء الحقول واستخدام `as any`.

**الهدف الاستراتيجي:**
تحويل النظام القائم إلى منصة أعمال ذكية (**Intelligent Business Platform**) متكاملة بدون هدم النظام أو إعادة بنائه من الصفر، من خلال فصل طبقة الأعمال (Domain/Service Layer) عن طبقة العرض (UI)، وتأسيس محرك محاسبي مركزي غير قابل للكسر، وسجل مخزون موحد (FIFO Inventory Ledger)، مع التجهيز الكامل لقاعدة بيانات PostgreSQL والامتثال لمعايير ZATCA والذكاء الاصطناعي.

---

### 2. تحليل البنية الحالية (Current Architecture Breakdown)

| الطبقة / المكون | الحالة الحالية في الشيفرة | التقييم المعماري |
| :--- | :--- | :--- |
| **Frontend Framework** | React 19, TypeScript, Vite 6, Tailwind CSS v4 | حديث وسريع الاستجابة ومكتمل الواجهات |
| **State Management** | React Context (`StateContext.tsx`) يجمع الحالة وعمليات CRUD ومنطق الحسابات والترحيل | متضخم (Monolithic God Context) - يخل بمبدأ المسؤولية الواحدة (SRP) |
| **Data Persistence** | `localStorage` فقط عبر 25 مفتاحاً منفصلاً داخل خطافات `useEffect` | محلي ومؤقت - غير قابل للتوسع لمستخدمين متعددين أو فروع متزامنة |
| **Backend Layer** | خادم Express (`server.ts`) يعمل كوسيط لـ Vite مع نقطة `/api/gemini/generate` ومحاكاة وهمية لـ ERPNext | بدائي وغير مرتبط ببيانات النظام الفعلية أو العمليات المحاسبية |
| **Database / ORM** | غير موجود حالياً (لا توجد إعدادات Drizzle أو PostgreSQL في مسار التشغيل الفعلي) | يتطلب بناء طبقة الوصول للبيانات (Repositories & Drizzle ORM Schema) |
| **Double-Entry Engine** | دالة `postJournalEntry` مدمجة بالـ Context، وتقوم بإعادة حساب أرصدة الحسابات داخل `useEffect` | تعاني من Race Conditions وإلغاء للأرصدة الافتتاحية للذمم |
| **Inventory FIFO** | دالة تفريغ كميات الدفعات (`batches`) مدمجة برمجياً في دوال التوريد والمبيعات | جيدة الفكرة لكنها تفتقر لسجل قيود مخزنية غير قابل للتعديل (Stock Ledger) |
| **Sector Coupling** | خلط مصطلحات خاصة بمحامص البن (Green Coffee, Roasting Degree) مع بنية النظام الأساسية | يحد من قابلية المنصة لتكون ERP عام متعدد الأنشطة التجارية والصناعية |

---

### 3. مصفوفة التدقيق الشاملة (System Audit Matrix)

| # | Area | Current State | Evidence | Risk | Recommended Action | Priority |
|---|---|---|---|---|---|---|
| **1** | **Accounting Balance Sync** | يتم حساب أرصدة الحسابات تلقائياً من `journalEntries` في خطاف `useEffect`، متجاهلاً أرصدة الذمم الافتتاحية | `StateContext.tsx` (L438-472) يعيد تصفير رصيد `acc-1200` و `acc-2000` لأن قيود الافتتاح لا تشمل ذمم العملاء الفردية | تضارب محاسبي بين ميزان المراجعة وأستاذ العملاء/الموردين | اعتماد دفتر الأستاذ العام كمصدر وحيد للحقيقة، وتضمين قيود افتتاحية لجميع الحسابات الفرعية | **Critical (P0)** |
| **2** | **COGS & Inventory in Reports** | تقارير الأرباح والخسائر تعتمد على نسب تخمينية وهمية (45% و 55%) وقيم كاش ثابتة (125,000) | `ReportsModule.tsx` (L28-32, L72, L80) : `totalRawPurchases * 0.45` و `wholesaleCOGS = wholesaleRevenue * 0.55` | بيانات مالية مضللة لا تمثل التكلفة الحقيقية الناتجة عن حركات المستودعات | ربط التقارير الختامية بمخرجات المحرك المحاسبي المركزي وسجل تكاليف الدفعات الفعلية | **Critical (P0)** |
| **3** | **Roastery Module Integration** | نموذج تشغيل المحمصة يرسل حقولاً بأسماء مخالفة لتعريف النوع (`greenItemId`, `greenQty`) ويستخدم `as any` | `RoasteryModule.tsx` (L63-76) مقارنة بـ `StateContext.tsx` (L723) و `types.ts` (L164-179) | فشل سحب البن الخام من دفعات المستودع وظهور خطأ للمستخدم بصمت | توحيد واجهات Domain Models والتخلص من `as any` واستخدام عقود موحدة لوظائف الإنتاج | **High (P1)** |
| **4** | **Grinding Module Integration** | نموذج تشغيل المطحنة يرسل حقولاً مخالفة للنوع (`roastedItemId`, `groundItemId`) ويستخدم `as any` | `GrindingModule.tsx` (L59-70) يفشل في مطابقة `inputBatchNumber` في `StateContext.tsx` (L787) | فشل خصم البن المحمص وإنتاج البن المطحون في المخزن | تصحيح توجيه البيانات وحقول الباتش وتغذية حركات المخزون عبر أمر تشغيل موحد | **High (P1)** |
| **5** | **Orphaned Component (DailySales)** | وجود مكون ضخم (716 سطراً) للمبيعات اليومية غير مدمج وغير مستخدم داخل `App.tsx` | `DailySalesModule.tsx` موجود في المجلد لكنه غائب تماماً عن واجهة التطبيق والتنقل | هدر في الكود، تشتت مصادر العمليات، وتكرار منطق البيع مع `PosModule` و `WholesaleModule` | تصنيف المكون كـ Deprecated ودمج خصائص الإرجاع والتحويلات الخاصة به في `SalesModule` | **Medium (P2)** |
| **6** | **Customer Management Duplication** | ازدواجية كاملة بين `CustomersModule.tsx` و `CustomerLedgerModule.tsx` في إدارة بطاقات العملاء وسندات القبض | كلا الملفين ينشئ نماذج تحرير عملاء وسندات قبض بحسابات منفصلة | تعديل بيانات العميل من شاشة قد لا ينعكس بشكل صحيح، وتشتت تسجيل التحصيلات | توحيد إدارة العملاء (بطاقة العميل وكشف الحساب والتحصيلات) في وحدة متكاملة ذات صلاحيات | **High (P1)** |
| **7** | **Data Persistence & Concurrency** | تخزين كافة البيانات في `localStorage` بصيغة JSON بدون دعم للمعاملات المتزامنة (Transactions) | `StateContext.tsx` يحتوي على 25 خطاف `useEffect` لحفظ وتفريغ المتغيرات | فقدان البيانات عند مسح ذاكرة المتصفح، واستحالة العمل التعاوني متعدد المستخدمين | نقل إدارة البيانات إلى Backend Services مع دعم التخزين المؤقت المحلي (Local-First Sync) | **High (P1)** |
| **8** | **Hardcoded Account IDs** | شيفرة الشراء والبيع والتحويلات تعتمد على معرفات حسابات مبرمجة نصياً (`acc-1300`, `acc-2000`, إلخ) | `StateContext.tsx` (L930-970, L1070-1110) | انهيار النظام عند قيام المستخدم بتعديل الدليل المحاسبي أو حذف الحسابات الافتراضية | ربط العمليات بحسابات الربط الافتراضية (Default Posting Accounts) من إعدادات الشركة | **High (P1)** |
| **9** | **Role-Based Access Control (RBAC)** | الصلاحيات مخزنة في الذاكرة المحلية والتحقق منها يتم على مستوى الواجهة الرسومية فقط | `StateContext.tsx` (L1435-1455) | انعدام الأمان الحقيقي؛ يمكن لأي مستخدم تجاوز قيود الواجهة | نقل التحقق من الصلاحيات إلى طبقة الخدمات والـ API مع حماية مسارات العمليات | **Medium (P2)** |
| **10** | **Domain Generalization** | ارتباط جوهر النظام بالقهوة والبهارات بشكل مبرمج داخل الجداول والقوائم المنسدلة | `types.ts` (ItemCategory enum) و `novaroInitialData.ts` | صعوبة تكييف النظام مع أنشطة أخرى (تجزئة، تصنيع عام، مواد غذائية أخرى) | عزل قطاع المحامص والمطاحن كموديول قطاعي متخصص (Specialized Domain Plugin) فوق محرك تصنيع عام | **Medium (P2)** |

---

### 4. تصنيف عناصر النظام (Keep / Refactor / Replace / Migrate / Add / Deprecate)

#### أ. عناصر يجب الإبقاء عليها (KEEP):
1. **نظام التصميم وهوية الواجهة (`DESIGN_SYSTEM.md` & `index.css`):** الالتزام الصارم بالتصميم المؤسسي الداعم للعربية والإنجليزية والخطوط والمسافات.
2. **جدول البيانات المؤسسي (`src/components/common/ERPTable.tsx`):** مكون عالي الجودة يحتوي على تصفية، بحث، ترتيب، تصدير، وتخصيص الأعمدة.
3. **هيكل الدليل المحاسبي الشجري (`AccountingModule.tsx - COA Tree`):** العرض الهرمي للحسابات مع أرصدتها وحالاتها.
4. **واجهة نقطة البيع السريعة (`src/components/PosModule.tsx`):** تجربة مستخدم جيدة للكاشير وطباعة الإيصالات.
5. **مكونات بطاقات الإحصاءات ولوحة المعلومات (`DashboardOverview.tsx`):** مقاييس الأداء الرئيسية والرسوم البيانية.

#### ب. عناصر يجب إعادة هيكلتها (REFACTOR):
1. **`StateContext.tsx`:** تفكيكه تدريجياً عبر نقل منطق الأعمال (Business Logic) إلى Services مستقلة، ليقتصر دوره على التنسيق وإدارة واجهة المستخدم (UI State Adapter).
2. **`AccountingModule.tsx`:** فصل دوال الترحيل، سندات القيد، وإقفال الفترات المالية إلى `AccountingService` مع ضمان عدم ترحيل أي قيد غير متوازن.
3. **`RoasteryModule.tsx` & `GrindingModule.tsx`:** تصحيح العقود ونقل منطق هدر التحميص ونعومة الطحن إلى خدمة تصنيع متخصصة تلبي شروط الأنواع المكتوبة بصرامة.
4. **`InventoryModule.tsx`:** إعادة بناء إدارة المخزون لتعمل بموجب قيود حركة المستودعات المزدوجة (Double-Entry Stock Ledger).

#### ج. عناصر يجب استبدالها (REPLACE):
1. **معادلات تقارير الأرباح في `ReportsModule.tsx`:** استبدال النسب المئوية الوهمية (`* 0.45` و `* 0.55`) باستعلامات فعلية عن حسابات الإيرادات وتكلفة البضاعة المباعة ومصاريف التشغيل.
2. **نظام التخزين المحلي كمرجع أخير (`localStorage`):** استبداله بـ Repository Pattern يقرأ ويكتب عبر API Gateway متصل بقاعدة بيانات حقيقية، مع الاحتفاظ بذاكرة تخزين محلي فقط للأوفلاين.

#### د. عناصر يجب ترحيلها (MIGRATE):
1. **بيانات التهيئة (`src/data/novaroInitialData.ts`):** تحويلها إلى بذور بيانات SQL/Drizzle معالجة محاسبياً بدقة لضمان توازن ميزان المراجعة بالكامل منذ اللحظة الأولى.
2. **عقود البيانات والأنواع (`src/types.ts`):** تقسيم الملف إلى وحدات فرعية حسب الـ Bounded Contexts (مثل `accounting.types.ts`, `inventory.types.ts`, `manufacturing.types.ts`).

#### هـ. عناصر يجب إضافتها (ADD):
1. **Central Accounting Engine:** محرك تدقيق وترحيل القيود، والتحقق من القيد المزدوج، وقفل الفترات، وسندات التسوية.
2. **Stock Valuation & Inventory Ledger:** سجل حركات المخزون التفصيلي لتتبع كل صنف وارد ومنصرف بتكلفته وتاريخ انتهاء صلاحيته.
3. **Domain Contracts & Repository Interfaces:** واجهات برمجية صارمة تفصل الشيفرة عن طريقة التخزين.
4. **Multi-Tenancy & Audit Logging Engine:** تسجيل تفصيلي لكل عملية تعديل مع هوية المستخدم والوقت والقيمة السابقة والحالية.

#### و. عناصر يجب إيقافها / إهمالها (DEPRECATE):
1. **`src/components/DailySalesModule.tsx`:** مكون مهمل وغير مدمج بالواجهة؛ يجب أرشفته أو دمجه ضمن مسار المبيعات الموحد.
2. **استخدام `as any` في استدعاءات الخدمات والوظائف:** منع التحايل على TypeScript لضمان موثوقية وقت التشغيل (Runtime Reliability).

---

### 5. تحليل مخاطر المحاسبة والمخزون (Deep Dives)

#### أ. المخاطر المحاسبية (Accounting Integrity Risks)
1. **خطر إعادة حساب الأرصدة التلقائي (Overwriting Stored Balances):**  
   في `StateContext.tsx`، يتم تصفير وحساب رصيد الحسابات بمجرد تعديل مصفوفة `journalEntries`. إذا تم إنشاء قيد افتتاحي لا يتطابق بالمليمتر مع أرصدة العملاء والموردين، تتلاشى أرصدة الحسابات الدائنة والمدينة وتظهر كصفر في الدليل المحاسبي.
2. **غياب تدقيق التوازن المسبق في جميع العمليات التلقائية:**  
   عمليات شراء المخزون، والبيع، والإهلاك تنشئ قيوداً برمجياً. في حال وجود خطأ في حساب الضريبة أو الخصم، قد ينشأ قيد غير متوازن، مما يفسد ميزان المراجعة.
3. **غياب قفل الفترات المالية (Accounting Period Lock):**  
   يمكن للمستخدم حالياً إدخال قيود أو تعديل فواتير في تواريخ سابقة لفترة تم إقفالها مالياً.

#### ب. مخاطر المخزون (Inventory Integrity Risks)
1. **خصم المخزون دون قيد مخزني مقابل:**  
   يتم خصم كميات الباتشات في الذاكرة مباشرة دون إنشاء سند إخراج مخزني موثق برقم تسلسلي، مما يجعل تتبع الفروقات مستحيلاً في حال وجود عجز أو تلف.
2. **تحديد يدوي لمعرفات المستودعات:**  
   تحويل المخزون يفترض معرفات ثابتة (`wh-1`, `wh-2`) بدلاً من التحقق من رصيد كل صنف داخل كل مستودع على حدة.

---

### 6. مسار الترحيل الموصى به (Recommended Migration Sequence)
1. **Phase 0 (الوضع الراهن):** استكمال وثائق التدقيق، المعمارية المستهدفة، خارطة الطريق، وخطة الاختبارات، مع التأكد التام من استقرار البناء الحالي (`build & lint`).
2. **Phase 1 (تثبيت الأساسات - Foundation Hardening):** نمذجة العقود، إصلاح تناقضات الأنواع البرمجية، وتأسيس هيكل Clean Architecture.
3. **Phase 2 (المحرك المحاسبي والمخزني المركزي):** تشغيل محرك القيد المزدوج وسجل المستودعات، وربط التقارير المالية بالأرقام الحقيقية.
4. **Phase 3 (تفكيك الوحدات وفصل الاختصاص):** فصل موديولات التحميص والطحن في خدمات مستقلة وتوحيد مسارات المبيعات والعملاء.
5. **Phase 4 (الانتقال لقاعدة البيانات):** تطبيق مخططات Drizzle/PostgreSQL، وإطلاق الـ Backend API، وإلغاء الاعتماد الحصري على `localStorage`.
6. **Phase 5 (الذكاء الاصطناعي والتوسع):** دمج وكيل الذكاء الاصطناعي للتدقيق المحاسبي والتنبؤ بالطلب والامتثال لمتطلبات الفاتورة الإلكترونية ZATCA.
