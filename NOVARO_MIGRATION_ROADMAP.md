# NOVARO Intelligent Business Platform
## خارطة طريق التحول والترحيل المتدرج (Migration Roadmap)
**النطاق:** خطة التحول المرحلي من NOVARO ERP 1.0 (Local Prototype) إلى NOVARO 2.0 (Intelligent Business Platform)  
**الاستراتيجية:** التحديث المتدرج المتوازي (Strangler Fig Pattern) بدون توقف للنظام، بدون كسر للميزات القائمة، وبدون إعادة بناء من الصفر.

---

### 1. مبادئ التحول غير القابلة للتفاوض (Non-Negotiable Principles)
1. **الاستمرارية التشغيلية:** كل مرحلة تنتهي بتطبيق يعمل بنسبة 100% ويجتاز اختبارات البناء والـ Lint بنجاح.
2. **عدم كسر الواجهات القائمة:** المكونات الحالية والشاشات والتنقل ونظام التصميم (`DESIGN_SYSTEM.md`) تظل سليمة ومستقرة.
3. **الحماية التراجعية (Rollback Safety):** كل خطوة هندسية قابلة للرجوع إليها بدون فقدان أي بيانات تم إدخالها.
4. **الانتقال خطوة بخطوة:** لا يتم الانتقال من مرحلة إلى المرحلة التي تليها إلا بعد اعتمادها واجتياز معايير الجودة (Certification Checklist).

---

### 2. المراحل الست للتحول المتدرج (The 6-Phase Master Plan)

```
  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │   PHASE 0    │ ──> │   PHASE 1    │ ──> │   PHASE 2    │
  │ Audit & Plan │     │  Foundation  │     │ Core Engines │
  └──────────────┘     └──────────────┘     └──────────────┘
                                                   │
  ┌──────────────┐     ┌──────────────┐            │
  │   PHASE 5    │ <── │   PHASE 4    │ <──────────┘
  │ AI & Scaling │     │  PostgreSQL  │     ┌──────────────┐
  └──────────────┘     └──────────────┘ <── │   PHASE 3    │
                                            │ Decoupling   │
                                            └──────────────┘
```

---

#### المرحلة 0: التدقيق وتثبيت الأساسات الحالية (Phase 0 — Audit & Baseline Stabilization)
- **الهدف:** توثيق النظام بالكامل، اكتشاف الثغرات وحالات التضارب البرمجي، التحقق من استقرار البناء، ووضع خطة الترحيل.
- **المخرجات:**
  1. `NOVARO_PLATFORM_AUDIT.md` (تم إنجازه).
  2. `NOVARO_TARGET_ARCHITECTURE.md` (تم إنجازه).
  3. `NOVARO_MIGRATION_ROADMAP.md` (هذه الوثيقة).
  4. `NOVARO_CERTIFICATION_PLAN.md` (خطة فحص الجودة والاعتماد).
- **الإجراءات التنفيذية المباشرة في Phase 0:**
  - التحقق من خلو المشروع من أخطاء الـ Lint والبناء الحالية (`npm run lint` & `npm run build`).
  - تشخيص التضاربات في استدعاءات `addRoastingJob` و `addGrindingJob` داخل `StateContext.tsx`.
- **معيار اكتمال المرحلة 0:** اعتماد الوثائق الأربع واجتياز فحص البناء دون أخطاء.

---

#### المرحلة 1: تحصين النطاق والعقود الصارمة (Phase 1 — Foundation Hardening)
- **الهدف:** تأسيس بنية Clean Architecture وهيكلة ملفات النطاق وإصلاح تناقضات الـ TypeScript.
- **المهام الرئيسية:**
  1. إنشاء مجلدات النطاق الأساسية:
     - `src/core/domain/accounting/`
     - `src/core/domain/inventory/`
     - `src/core/domain/manufacturing/`
     - `src/core/domain/sales/`
  2. تعريف عقود الواجهات (Interfaces & Repository Contracts) لضمان فصل منطق الأعمال عن `StateContext`.
  3. تصحيح عدم تطابق الحقول بين `RoasteryModule.tsx` و `GrindingModule.tsx` ومجسم البيانات في `types.ts`، والتخلص من استخدام `as any`.
  4. وضع مصفوفة القيود الافتتاحية الصحيحة لضمان مطابقة الذمم المدينة والدائنة مع الدليل المحاسبي.
- **معيار اكتمال المرحلة 1:** عدم وجود أي خطأ في أنواع TypeScript، واكتمال عقود النطاق الأساسية.

---

#### المرحلة 2: المحركات المركزية الموحدة (Phase 2 — Central Engines)
- **الهدف:** بناء المحرك المحاسبي المركزي الموحد ومحرك المخزون المعتمد على حركات الدفعات الحقيقية.
- **المهام الرئيسية:**
  1. **Central Accounting Engine:**
     - منع أي عملية ترحيل لقيد غير متوازن ($\text{Debit} \neq \text{Credit}$).
     - إيقاف الحذف أو التعديل المباشر للقيود المرحلة، واعتماد آلية القيد العكسي (Reversal Entry).
     - توحيد احتساب أرصدة الحسابات المالية عبر دفتر الأستاذ دون التأثر بحالة إعادة الرندر.
  2. **Inventory Stock Ledger:**
     - تسجيل كل حركة إدخال، إخراج، تحويل، أو تسوية في سجل حركات مخزنية موحد.
     - ربط تكلفة البضاعة المباعة (COGS) بسعر التكلفة الحقيقي لكل دفعة بموجب الوارد أولاً يصرف أولاً (FIFO).
  3. **إصلاح تقارير الأرباح والخسائر (`ReportsModule.tsx`):**
     - استئصال المعادلات التقديرية (`* 0.45` و `* 0.55` ورصيد الكاش الوهمي `125,000`).
     - اشتقاق الأرقام المالية بالكامل من قيود دفتر الأستاذ وسجلات التكلفة المخزنية.
- **معيار اكتمال المرحلة 2:** تطابق ميزان المراجعة مع أستاذ الحسابات وتقارير الأرباح بدون أدنى فارق هللة واحدة.

---

#### المرحلة 3: تفكيك وتوحيد الموديولات (Phase 3 — Domain Decoupling & Module Unification)
- **الهدف:** فصل منطق الأعمال من `StateContext` ونقله إلى طبقة الخدمات (Services)، وتوحيد الشاشات المتكررة.
- **المهام الرئيسية:**
  1. توحيد شاشات العملاء: دمج وتكامل `CustomersModule.tsx` مع `CustomerLedgerModule.tsx` في مسار متكامل ومحمي بصلاحيات.
  2. توحيد شاشات المبيعات: ربط `WholesaleModule.tsx` و `PosModule.tsx` بمحرك الفواتير الموحد وإلغاء الازدواجية البرمجية.
  3. أرشفة المكون المهمل `DailySalesModule.tsx` وتوثيق حالته لتفادي تشتت المطورين.
  4. عزل موديولي التحميص والطحن كموديولات قطاعية متخصصة (Specialized Roastery Plugins) تعتمد على المحرك التصنيعي العام.
- **معيار اكتمال المرحلة 3:** تقليص حجم `StateContext.tsx` ليصبح مجرد وسيط خفيف (Thin UI State Layer)، وإنجاز جميع العمليات عبر Services متخصصة.

---

#### المرحلة 4: ربط قاعدة البيانات المستدامة (Phase 4 — PostgreSQL & Persistence Layer)
- **الهدف:** نقل النظام من الاعتماد المطلق على `localStorage` إلى قاعدة بيانات علائقية سحابية (PostgreSQL) باستخدام Drizzle ORM مع دعم العمل دون اتصال (Local-First Offline Sync).
- **المهام الرئيسية:**
  1. إعداد مخطط الجداول في `src/db/schema.ts` و `drizzle.config.ts`.
  2. بناء مسارات الـ REST API في `server.ts` لإدارة العمليات المحاسبية والمخزنية بصورة آمنة.
  3. توفير أداة هجرة البيانات بنقرة واحدة (One-Click Migration Tool) لنقل بيانات المستخدم من `localStorage` إلى PostgreSQL دون أي فقد للبيانات.
  4. تطبيق نظام المعاملات البرمجية الصارمة (ACID Transactions) لمنع الحركات الجزئية عند انقطاع الاتصال.
- **معيار اكتمال المرحلة 4:** عمل التطبيق بكفاءة على قاعدة بيانات سحابية حقيقية مع وجود حماية كاملة ضد تضارب البيانات المتزامنة.

---

#### المرحلة 5: الذكاء الاصطناعي والتوسع المؤسسي (Phase 5 — AI Copilot & Scaling)
- **الهدف:** إطلاق مزايا الذكاء الاصطناعي، دعم الفواتير المتوافقة مع ZATCA المرحلة الثانية، وتوسيع دعم الفروع والمنشآت المتعددة.
- **المهام الرئيسية:**
  1. تفعيل المساعد الذكي (AI Financial Auditor) باستخدام Gemini API لمراجعة القيود المحاسبية وكشف الأخطاء والشذوذ المالي.
  2. التنبؤ الذكي بحجم استهلاك المواد الخام وإعادة الطلب (Smart Inventory Forecasting).
  3. التحقق من توافق الفواتير الضريبية ومخرجات الـ QR Code مع اشتراطات هيئة الزكاة والضريبة والجمارك (ZATCA Phase 2).
  4. تدشين إمكانية إدارة فروع ومستودعات متعددة برخص وصلاحيات معزولة تماماً.
- **معيار اكتمال المرحلة 5:** نظام ذكي متكامل يعمل بكفاءة مؤسسية ويلبي أعلى معايير الحوكمة المالية والصناعية.
